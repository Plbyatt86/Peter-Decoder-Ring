// Write your tests here!
const { expect } = require("chai");
const { substitution } = require("../src/substitution");

describe("substitution", () => {
  it("should encode a message properly", () => {
    // Test for encoding a message
  });

  it("should decode a message properly", () => {
    // Test for decoding a message
  });

  it("should handle unique substitution alphabet", () => {
    // Test to make sure substitution alphabet is unique
  });

  it("should return false if substitution alphabet is not exactly 26 characters", () => {
    // Test to make sure the substitution alphabet is exactly 26 characters
  });
});
